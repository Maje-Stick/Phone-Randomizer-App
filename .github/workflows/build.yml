name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          gradle-version: '8.11.1'

      - name: Decode the signing keystore
        env:
          KEYSTORE_B64: ${{ secrets.SIGNING_KEYSTORE_BASE64 }}
        run: |
          if [ -z "$KEYSTORE_B64" ]; then
            echo "SIGNING_KEYSTORE_BASE64 secret is missing." >&2
            exit 1
          fi
          # tr strips any newlines a text editor introduced while copying.
          printf '%s' "$KEYSTORE_B64" | tr -d '[:space:]' | base64 -d > "$RUNNER_TEMP/signing.p12"
          if [ ! -s "$RUNNER_TEMP/signing.p12" ]; then
            echo "Keystore decoded to an empty file. The secret is probably truncated." >&2
            exit 1
          fi
          echo "SIGNING_KEYSTORE_PATH=$RUNNER_TEMP/signing.p12" >> "$GITHUB_ENV"

      # Both outputs come from the release build type, so the APK you install is
      # the same code path Play receives -- R8 and all. A debug APK would not
      # exercise the shrinker at all, which is where release-only breakage hides.
      - name: Build the release APK and bundle
        env:
          SIGNING_STORE_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
          SIGNING_KEY_ALIAS: ${{ secrets.SIGNING_KEY_ALIAS }}
          SIGNING_KEY_PASSWORD: ${{ secrets.SIGNING_KEY_PASSWORD }}
        run: gradle assembleRelease bundleRelease --no-daemon --stacktrace

      - name: Upload the APK
        uses: actions/upload-artifact@v5
        with:
          name: randomizer-release-apk
          path: app/build/outputs/apk/release/*.apk
          if-no-files-found: error

      - name: Upload the Play bundle
        uses: actions/upload-artifact@v5
        with:
          name: randomizer-play-bundle
          path: app/build/outputs/bundle/release/*.aab
          if-no-files-found: error
